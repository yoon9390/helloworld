# 🛰 기획서 — 「수식 계보도」(가제) / Equation Genealogy

## 한 줄 컨셉
이론물리를 *하나의 거대한 옵시디언 그래프*로 본다: **정리/가설이 기호를 낳고, 그 기호가 새 방정식에 끼워지는** 계보를 클릭으로 따라 점등시키는 인터랙티브 탐색기.

## 핵심 상호작용
- 방정식 입력 또는 리스트에서 선택 → 렌더링(KaTeX/MathJax).
- 기호 **호버** → 짧은 뜻(툴팁).
- 기호 **드래그/선택** → 한 단락 설명.
- 기호·뭉치 **클릭** → ① 그게 *처음 등장한* 방정식 + ② 이후 그 기호가 들어간 방정식 리스트(자연어 설명과 함께) 쫙.
- = 본질적으로 **노드(기호·방정식) + 엣지(등장/파생) 그래프 순회.** "클릭하면 다음 뉴런 다발 점등"이 정확히 graph traversal. (문과 클라이언트 말 맞음, UI는 진짜 단순함 ㅋㅋ)

## 데이터: 진짜 어려운 부분은 여기
UI는 쉬움. 핵심 난제는 *"식과 계보를 어디서 끌어오나".*
- **(A) 큐레이션 셋 (추천 시작점):** 「우주 산책」에 나온 식 15~30개를 손으로 정제한 작은 고품질 그래프. 정확하고 즉시 가능.
- **(B) Wikidata/Wikipedia:** 구조화 데이터(P 속성)로 일부 자동화 가능하나 노이즈·계보 정보 부족.
- **(C) nLab/arXiv/교과서:** 풍부하나 파싱·정합성 큰 작업.
- **(D) LLM 생성 + 사람 검수:** 빠른 초안, 단 검수 필수(오류·환각 주의).
- → **권장: A로 개념증명 → 반응 좋으면 B/D로 확장.**

## 이미 있나?
비슷한 건 있음(위키피디아, Wikidata, Detexify=손글씨→LaTeX, Physics Travel Guide, 각종 수식 사전). 근데 **"기호 계보를 그래프로, 처음 등장→이후 전파를 클릭으로 추적"**하는 *큐레이션 학습 탐색기*는 틈새 분명. 특히 하나의 학습 서사(우리 산책)에 묶으면 차별점 확실.

## 어디서 만드나 — code vs chat
- **개념증명(프로토타입): chat에서 충분.** 단일 React 아티팩트 하나 — 작은 JSON 그래프 + KaTeX 렌더 + 호버/클릭/점등. 식 10~20개로 체험까지 바로.
- **본 앱(대규모 데이터·검색·저장·기여): Claude Code 영역.** 프로젝트 구조, 데이터 파이프라인, 저장소, 배포.
- **추천 경로: chat 프로토타입 → 손맛 → Claude Code 승격.**

## 데이터 스키마 초안
```json
{
  "symbols": [
    { "id": "c", "gloss": "빛의 속도", "longDesc": "진공 광속, 약 3×10^8 m/s …",
      "firstAppearedIn": "maxwell", "usedIn": ["emc2", "schwarzschild", "energy_momentum"] }
  ],
  "equations": [
    { "id": "emc2", "latex": "E = mc^2", "name": "질량-에너지 등가",
      "year": 1905, "by": "Einstein", "symbols": ["E","m","c"],
      "desc": "질량과 에너지가 같은 것의 두 얼굴 …", "derivesFrom": ["sr_postulates"] }
  ]
}
```

## 🔬 '이거 되나요?' — 작가 추가 기능 타당성
공통 진실: **화면(시각화)은 다 쉬움. 어려운 건 늘 데이터(계보·등가·치환규칙)거나 자동추론(CAS/정리증명).** 큐레이션으로 시작하면 셋 다 v0~v1에서 맛보기 가능.

### 🔺 변형 삼단논법 (A=B, C=B ⟹ A=C) — 소크라테스 모듈
- **됨, 조건부.** 등식을 엣지로 저장하면 A=C 찾기는 그래프 경로탐색(쉬움).
- 단, *임의의* 식에서 자동으로 참인 새 등식을 만들려면 컴퓨터 대수 시스템(CAS) 수준 추론 필요.
- 큐레이션된 등가 그래프면 즉시 가능. **실제 예(v0 데모로 넣음):** S = k log W 와 S = A/4 가 S를 공유 → ∴ A/4 = k log W (블랙홀 넓이가 곧 미시상태의 로그!). 진짜 물리 결과라 데모로 딱.
- 판정: **v0에 큐레이션 데모 가능 / 자동화는 phase 2.**

### 🔄 텍스트 대치 (g → e/2(1+v))
- **됨.** 기호 치환은 CAS의 기본기. 웹에서 nerdamer·Algebrite 같은 클라이언트 JS CAS로 임의 치환 가능. 또는 큐레이션된 치환 규칙 테이블.
- v0에선 하드코딩 데모로 맛보기(예: E²=(mc²)²+(pc)² 에 m=0 대입 → E=pc).
- 판정: **v0 데모 가능 / 범용 치환은 CAS 라이브러리 붙이면 v1.**

### ⏳ 타임랩스 진화 (φ₀ 펼침 → 분기 → 합류)
- **시각화는 쉬움.** 유도 DAG에 연도/순서를 넣고 애니메이션. 
- 작가 직감("노드 정보 다 갖춰지면 역산→시간순 가능") — **맞음.** 빠진 건 *데이터(계보 체인)*지 알고리즘이 아님.
- 단, *자동 역산*(식만 보고 유도 과정 복원)은 자동 정리증명 영역이라 어려움. 사람이 계보를 한 번 깔아주면 재생은 공짜.
- 판정: **데이터만 갖추면 v1에서 충분히 멋짐.**

## 다음 액션
- [ ] 큐레이션 셋 v0 (산책 등장 식: E=mc², E²=(mc²)²+(pc)², 아인슈타인 방정식, ds², S=k log W, S=A/4 …)
- [ ] 노드/엣지 JSON 스키마 확정
- [ ] 단일 아티팩트 프로토타입(호버·클릭·점등)
- [ ] 반응 보고 Claude Code 승격 판단

문과 클라이언트 + 프로그래머 클로드 협업 프로젝트로 딱. 재밌음. 하자 ㅋㅋ 🛰
